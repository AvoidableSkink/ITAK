#include <iostream>

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;

//example of how to use the split function in utils
//    Region* region = nullptr;
//    std::string fields[3];
//    if (split(data, ',', fields, 3)) {
}